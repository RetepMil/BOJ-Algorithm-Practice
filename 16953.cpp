#include <iostream>
using namespace std;

#define MAX 100

int N;
int map[MAX][MAX];
bool visit[MAX][MAX];

const int dx[] = { -1, 1, 0, 0 };
const int dy[] = { 0, 0, -1, 1 };

void bfs() {
    return;
}

int main() {
    cin >> N;
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            cin >> map[i][j];
        }
    }

    for (int i = 0; i < 101; i++) {

    }

    return 0;
}